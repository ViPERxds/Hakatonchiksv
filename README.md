# 🤖 Телеграм-бот для обработки счетов

Автоматизированная система для извлечения структурированных данных из PDF-счетов и фото документов с помощью OCR технологий.

## ✨ Основные возможности

### 📄 Обработка документов
- **PDF-файлы**: Извлечение данных из электронных и отсканированных PDF
- **Фото счетов**: OCR-обработка изображений (JPG, PNG, BMP, TIFF)
- **Продвинутый OCR**: Tesseract с русско-английскими словарями и предобработкой изображений
- **Таблицы**: Автоматическое распознавание товарных позиций из таблиц

### 💼 Бизнес-функции
- **История счетов**: Хранение всех обработанных документов в SQLite базе
- **Статистика**: Аналитика по периодам, топ-поставщики, суммы
- **Excel-экспорт**: Выгрузка счетов и статистики в формате .xlsx
- **Уведомления**: Настраиваемые напоминания

### 🎯 Извлекаемые данные

#### Основная информация счета
- Номер счета, дата, срок действия
- Продавец и покупатель (название, ИНН, КПП, адрес, телефон)
- Банковские реквизиты (р/с, к/с, БИК, банк)

#### Финансовая информация
- Товарные позиции (наименование, кол-во, ед.изм, цена, сумма)
- Итоговая сумма без НДС
- НДС (ставка и сумма)
- Итоговая сумма с НДС
- Сумма прописью
- Валюта

#### Дополнительные данные
- Подписанты (директор, главный бухгалтер)
- Условия и положения
- Информация о самовывозе

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- Docker и Docker Compose (для контейнеризации)
- Telegram Bot Token (получите у [@BotFather](https://t.me/BotFather))

### Установка и запуск

#### Вариант 1: Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd hakaton
```

2. Создайте файл `.env`:
```bash
# Linux/Mac
cat > .env << EOF
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
API_URL=http://api:8000
DATABASE_URL=sqlite:///./data/invoices.db
EOF

# Windows PowerShell
@"
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
API_URL=http://api:8000
DATABASE_URL=sqlite:///./data/invoices.db
"@ | Out-File -FilePath .env -Encoding UTF8
```

3. Запустите сервисы:
```bash
docker-compose up --build
```

Сервисы будут доступны:
- 🌐 API: http://localhost:8000
- 🤖 Telegram Bot: автоматически подключится к Telegram
- 📊 API Docs: http://localhost:8000/docs

#### Вариант 2: Локальный запуск

1. Установите системные зависимости:

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y tesseract-ocr tesseract-ocr-rus poppler-utils libgl1-mesa-glx libglib2.0-0
```

**macOS:**
```bash
brew install tesseract tesseract-lang poppler
```

**Windows:**
- Установите [Tesseract OCR](https://github.com/UB-Mannheim/tesseract/wiki)
- Установите [Poppler](http://blog.alivate.com.au/poppler-windows/)
- Добавьте пути к Tesseract и Poppler в PATH

2. Установите Python-зависимости:
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

3. Создайте `.env` файл:
```bash
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
API_URL=http://localhost:8000
DATABASE_URL=sqlite:///./invoices.db
```

4. Запустите сервисы:

**Терминал 1 - API:**
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**Терминал 2 - Бот:**
```bash
python run_bot.py
```

## 📱 Использование бота

### Команды

- `/start` - Запуск бота и отображение меню
- `/help` - Справка по использованию
- `/history [N]` - История последних N счетов (по умолчанию 10)
- `/stats [days]` - Статистика за указанный период (по умолчанию 30 дней)
- `/export` - Экспорт всех счетов в Excel
- `/export stats` - Экспорт статистики в Excel
- `/settings` - Настройки уведомлений

### Интерактивные кнопки

Бот предоставляет удобную клавиатуру с кнопками:
- 📄 **Отправить PDF** - Подсказка для отправки документа
- 📊 **Статистика** - Просмотр статистики
- 📜 **История** - История обработанных счетов
- 📥 **Экспорт в Excel** - Выгрузка данных
- ℹ️ **Помощь** - Справочная информация

### Процесс обработки

1. Отправьте PDF-файл или фото счета боту
2. Дождитесь обработки (5-15 секунд)
3. Получите:
   - Краткую информацию о счете
   - Полные данные в формате JSON
   - Кнопки для быстрых действий

## 🏗️ Архитектура проекта

```
hakaton/
├── app/                          # Основной код приложения
│   ├── main.py                   # FastAPI сервер
│   ├── bot.py                    # Telegram бот
│   ├── pdf_processor.py          # Обработка PDF и OCR
│   ├── data_extractor.py         # Извлечение данных (regex)
│   ├── database.py               # SQLAlchemy модели
│   ├── stats.py                  # Статистика
│   ├── excel_export.py           # Экспорт в Excel
│   ├── notifications.py          # Уведомления
│   ├── scheduler.py              # Планировщик задач
│   └── ml_model.py               # ML валидация (опционально)
├── models/                       # ML модели
│   ├── invoice_classifier.pkl    # Обученная модель
│   └── model_metrics.json        # Метрики модели
├── notebooks/                    # Jupyter ноутбуки
│   ├── training.ipynb            # Обучение ML модели
│   └── demo.ipynb                # Демонстрация работы
├── data/                         # Директория для базы данных
├── uploads/                      # Временные файлы
├── Dockerfile                    # Docker-образ
├── docker-compose.yml            # Оркестрация сервисов
├── requirements.txt              # Python зависимости
├── run_bot.py                    # Запуск бота
└── README.md                     # Этот файл
```

## 🔧 API Endpoints

### Основные эндпоинты

#### `GET /`
Информация о сервисе

#### `GET /health`
Проверка здоровья сервиса

#### `POST /process-invoice`
Обработка PDF-счета

**Параметры:**
- `file`: PDF-файл (multipart/form-data)
- `user_id`: ID пользователя (опционально)
- `user_name`: Имя пользователя (опционально)

**Ответ:**
```json
{
  "success": true,
  "data": {
    "invoice": {
      "number": "650",
      "date": "16.09.2025",
      "validity_period": "5 банковских дней"
    },
    "supplier": {
      "company_name": "ООО ТД \"Промэлектроника\"",
      "inn": "6678047070",
      "kpp": "667801001",
      "address": "620142, г. Екатеринбург...",
      "phone": "83432224455",
      "bank_details": {...}
    },
    "customer": {...},
    "line_items": [...],
    "financial_summary": {
      "subtotal_without_vat": 243375.00,
      "vat_rate": "20%",
      "vat_amount": 48675.00,
      "total_with_vat": 292050.00,
      "currency": "RUB"
    },
    ...
  },
  "message": "Данные успешно извлечены"
}
```

#### `POST /process-image`
Обработка изображения счета (OCR)

**Параметры:**
- `file`: Изображение (JPG, PNG, BMP, TIFF)
- `user_id`, `user_name`: Опционально

#### `GET /history/{user_id}`
История обработанных счетов

**Query параметры:**
- `limit`: Количество записей (по умолчанию 10)

#### `GET /stats/{user_id}`
Статистика пользователя

**Query параметры:**
- `days`: Период в днях (по умолчанию 30)

#### `GET /export/excel/{user_id}`
Экспорт счетов в Excel

#### `GET /export/stats/{user_id}`
Экспорт статистики в Excel

**Query параметры:**
- `days`: Период в днях (по умолчанию 30)

### Swagger документация

После запуска API доступна интерактивная документация:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 🛠️ Технологический стек

### Backend
- **FastAPI** - Современный веб-фреймворк для API
- **Python 3.11+** - Язык программирования
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - База данных

### Telegram Bot
- **python-telegram-bot** - Библиотека для Telegram Bot API
- **aiohttp** - Асинхронные HTTP-запросы

### PDF и OCR
- **pdfplumber** - Извлечение текста и таблиц из PDF
- **PyPDF2** - Дополнительная работа с PDF
- **pdf2image** - Конвертация PDF в изображения
- **Tesseract OCR** - Распознавание текста с изображений
- **OpenCV** - Предобработка изображений
- **Pillow** - Работа с изображениями

### ML и обработка данных
- **scikit-learn** - Машинное обучение (RandomForest)
- **pandas** - Обработка данных
- **numpy** - Численные вычисления

### Excel и визуализация
- **openpyxl** - Создание Excel-файлов
- **xlsxwriter** - Дополнительный экспорт
- **matplotlib, seaborn** - Визуализация данных (Jupyter)

### Контейнеризация
- **Docker** - Контейнеризация приложения
- **Docker Compose** - Оркестрация микросервисов

## 📊 Обработка данных

### OCR Pipeline

Система использует многоступенчатый подход для максимальной точности:

1. **Попытка прямого извлечения** (pdfplumber, PyPDF2)
2. **Предобработка изображений** (при необходимости):
   - Конвертация в градации серого
   - Увеличение контраста
   - Удаление шума (деноизинг)
   - Бинаризация (OTSU)
   - Морфологические операции
3. **OCR с разными режимами** (PSM 6, 11, 12)
4. **Fallback механизмы** при низком качестве

### Извлечение данных

1. **Регулярные выражения** - Основной метод для структурированных данных
2. **Контекстный анализ** - Поиск данных в правильных секциях документа
3. **ML валидация** (опционально) - Проверка извлеченных данных классификатором

### База данных

SQLite хранит:
- Метаданные счетов (номер, дата, сумма)
- Полные извлеченные данные (JSON)
- Информацию о пользователях
- Настройки уведомлений

## 📓 Jupyter Ноутбуки

### training.ipynb
Обучение ML модели для валидации извлеченных данных:
- Извлечение признаков (TF-IDF + статистические)
- Обучение RandomForest классификатора
- Оценка метрик (Accuracy, Precision, Recall, F1-Score)
- Визуализация результатов
- Сохранение модели

### demo.ipynb
Демонстрация работы системы:
- Примеры обработки PDF
- Извлечение данных
- Анализ результатов
- ML валидация

## 🔒 Безопасность

- ✅ Автоматическое удаление временных файлов после обработки
- ✅ Валидация типов и размеров файлов
- ✅ Обработка ошибок и исключений
- ✅ Изоляция сервисов в Docker контейнерах
- ✅ Переменные окружения для секретов

## 🐛 Устранение неполадок

### Бот не запускается
```bash
# Проверьте токен в .env
cat .env | grep TELEGRAM_BOT_TOKEN

# Проверьте доступность API
curl http://localhost:8000/health
```

### OCR не работает
```bash
# Проверьте установку Tesseract
tesseract --version

# Проверьте русский язык
tesseract --list-langs | grep rus
```

### Docker проблемы
```bash
# Пересоберите контейнеры
docker-compose down
docker-compose build --no-cache
docker-compose up

# Проверьте логи
docker-compose logs -f
```

### Низкая точность распознавания

**Для PDF:**
- Убедитесь, что PDF содержит текстовый слой
- Для отсканированных PDF увеличьте DPI сканирования (300+)

**Для фото:**
- Сделайте четкое фото при хорошем освещении
- Избегайте бликов и теней
- Держите камеру параллельно документу
- Убедитесь, что весь текст виден

## 🚧 Планы развития

- [ ] Поддержка английских счетов
- [ ] Распознавание рукописного текста
- [ ] Интеграция с бухгалтерскими системами (1С)
- [ ] Веб-интерфейс для управления
- [ ] Поддержка других мессенджеров (WhatsApp, Viber)
- [ ] Расширенная аналитика и дашборды
- [ ] Автоматическая сверка счетов
- [ ] API для интеграции с внешними системами

## 📝 Лицензия

Этот проект создан для образовательных и хакатонных целей.

## 🤝 Вклад в проект

Приветствуются Pull Request'ы для улучшения проекта:

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📧 Поддержка

При возникновении вопросов или проблем:
- Создайте Issue в репозитории
- Опишите проблему с примерами
- Приложите логи при необходимости

---

Сделано с ❤️ 
